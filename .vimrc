" ~/.vimrc — dotfiles 受管。最小可用，够改配置文件就行
set nocompatible
syntax on
set number
set mouse=a
set encoding=utf-8
set expandtab shiftwidth=2 tabstop=2 softtabstop=2
set ignorecase smartcase incsearch hlsearch
set backspace=indent,eol,start
set nobackup nowritebackup noswapfile
